include_directories(evtrec)
include_directories(g4sim)
include_directories(ndtfdst)

add_subdirectory(apps)
add_subdirectory(g4sim)
add_subdirectory(evtrec)
add_subdirectory(ndtfdst)
add_subdirectory(config)

